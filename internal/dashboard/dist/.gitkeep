# Placeholder. The contents of this directory are generated by
# `task ui:embed` from dashboard/dist/. The directory itself must exist
# at compile time so //go:embed all:dist resolves on a fresh clone before
# anyone has run the dashboard build.
